name: UnlockAchievementCmd
version: v1
summary: Command to unlock an achievement for a player.
producers:
  - GameFrontend
consumers:
  - PlayerGrain
payload:
  type: object
  properties:
    PlayerId:
      type: string
      format: uuid
    AchievementId:
      type: string
      format: uuid